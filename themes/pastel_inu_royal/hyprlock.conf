input-field {
	monitor = 
		size = 215, 50
		outline_thickness = 3
		dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
		dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
		dots_center = false
		dots_rounding = -1 # -1 default circle, -2 follow input-field rounding
		outer_color = rgb(135,100,199)
		inner_color = rgb(36, 39, 58)
		font_color = rgb(135,100,199)
		fade_on_empty = true
		fade_timeout = 3000 # Milliseconds before fade_on_empty is triggered.
		placeholder_text = <i>Input Password...</i> # Text rendered in the input box when it's empty.
		hide_input = false
		rounding = -1 # -1 means complete rounding (circle/oval)
		check_color = rgb(135,100,199)
		fail_color = rgb(230, 93, 79)
		fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> # can be set to empty
		fail_transition = 300 # transition time in ms between normal outer_color and fail_color
		capslock_color = rgb(221, 161, 111)
		numlock_color = -1
		bothlock_color = rgb(221, 161, 111) # when both locks are active. -1 means don't change outer color (same for above)
		invert_numlock = false # change color if numlock is off
		swap_font_color = false # see below

		position = -540, -20
		halign = center
		valign = center
}

label {
	monitor = 
		text = $TIME
		text_align = dots_center
		color = rgb(135,100,199)
		font_size = 50
		font_family = ComicShannsMono Nerd Font
		position = 510, -20
		halign = center
		valign = center

}
